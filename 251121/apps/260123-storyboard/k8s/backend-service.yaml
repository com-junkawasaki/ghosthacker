apiVersion: v1
kind: Service
metadata:
  name: storyboard-backend
  namespace: storyboard-editor
  labels:
    app: storyboard-backend
spec:
  type: ClusterIP
  ports:
    - port: 8081
      targetPort: 8081
      protocol: TCP
      name: http
  selector:
    app: storyboard-backend
