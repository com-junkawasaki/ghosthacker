import { createConnectTransport } from "@connectrpc/connect-web";
import { createPromiseClient } from "@connectrpc/connect";
// Note: These would be generated by protoc-gen-connect-es
// import { EditorService } from "../../backend/proto/editor_connect";

const transport = createConnectTransport({
  baseUrl: "http://localhost:8080",
});

// For now, exporting a mock or placeholder until code generation is run
export const client = {
  analyzeText: async (text: string) => {
    console.log("Analyzing text via gRPC:", text);
    // return client.analyzeText({ text });
    return { summary: "Analysis in progress...", entities: [] };
  }
};

