[package]
name = "rag-embedding"
version = "0.1.0"
edition = "2021"

[dependencies]
rag-openai = { path = "../rag-openai" }
rag-storage = { path = "../rag-storage" }
tokio = { version = "1", features = ["full"] }
serde_json = "1.0"
anyhow = "1.0"
