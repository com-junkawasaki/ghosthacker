[package]
name = "rag-openai-component"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
wit-bindgen = { version = "0.21", features = ["wasmtime-component-macro"] }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
anyhow = "1.0"

# Use the existing library code
rag-openai = { path = "../rag-openai" }

[profile.release]
opt-level = "z"
lto = true
codegen-units = 1
strip = true
