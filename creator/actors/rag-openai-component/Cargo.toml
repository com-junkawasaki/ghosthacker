[package]
name = "rag-openai-component"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
# Use the existing library code
# rag-openai = { path = "../rag-openai" }  # Temporarily disabled due to wasm compatibility issues

[profile.release]
opt-level = "z"
lto = true
codegen-units = 1
strip = true