// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.30.0
// source: manga_panels.sql

package sqlc

import (
	"context"

	"github.com/jackc/pgx/v5/pgtype"
)

const createMangaPanel = `-- name: CreateMangaPanel :one
INSERT INTO manga_panels (page_id, order_index, x, y, width, height, layout_type, prompt)
VALUES ($1, $2, $3, $4, $5, $6, $7, $8)
RETURNING id, page_id, order_index, x, y, width, height, layout_type, prompt, created_at, updated_at
`

type CreateMangaPanelParams struct {
	PageID     pgtype.UUID   `json:"page_id"`
	OrderIndex int32         `json:"order_index"`
	X          pgtype.Float8 `json:"x"`
	Y          pgtype.Float8 `json:"y"`
	Width      pgtype.Float8 `json:"width"`
	Height     pgtype.Float8 `json:"height"`
	LayoutType pgtype.Text   `json:"layout_type"`
	Prompt     pgtype.Text   `json:"prompt"`
}

func (q *Queries) CreateMangaPanel(ctx context.Context, arg CreateMangaPanelParams) (MangaPanel, error) {
	row := q.db.QueryRow(ctx, createMangaPanel,
		arg.PageID,
		arg.OrderIndex,
		arg.X,
		arg.Y,
		arg.Width,
		arg.Height,
		arg.LayoutType,
		arg.Prompt,
	)
	var i MangaPanel
	err := row.Scan(
		&i.ID,
		&i.PageID,
		&i.OrderIndex,
		&i.X,
		&i.Y,
		&i.Width,
		&i.Height,
		&i.LayoutType,
		&i.Prompt,
		&i.CreatedAt,
		&i.UpdatedAt,
	)
	return i, err
}

const deleteMangaPanel = `-- name: DeleteMangaPanel :exec
DELETE FROM manga_panels WHERE id = $1
`

func (q *Queries) DeleteMangaPanel(ctx context.Context, id pgtype.UUID) error {
	_, err := q.db.Exec(ctx, deleteMangaPanel, id)
	return err
}

const getMangaPanel = `-- name: GetMangaPanel :one
SELECT id, page_id, order_index, x, y, width, height, layout_type, prompt, created_at, updated_at
FROM manga_panels
WHERE id = $1
`

func (q *Queries) GetMangaPanel(ctx context.Context, id pgtype.UUID) (MangaPanel, error) {
	row := q.db.QueryRow(ctx, getMangaPanel, id)
	var i MangaPanel
	err := row.Scan(
		&i.ID,
		&i.PageID,
		&i.OrderIndex,
		&i.X,
		&i.Y,
		&i.Width,
		&i.Height,
		&i.LayoutType,
		&i.Prompt,
		&i.CreatedAt,
		&i.UpdatedAt,
	)
	return i, err
}

const listMangaPanels = `-- name: ListMangaPanels :many
SELECT id, page_id, order_index, x, y, width, height, layout_type, prompt, created_at, updated_at
FROM manga_panels
WHERE page_id = $1
ORDER BY order_index ASC
`

func (q *Queries) ListMangaPanels(ctx context.Context, pageID pgtype.UUID) ([]MangaPanel, error) {
	rows, err := q.db.Query(ctx, listMangaPanels, pageID)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	var items []MangaPanel
	for rows.Next() {
		var i MangaPanel
		if err := rows.Scan(
			&i.ID,
			&i.PageID,
			&i.OrderIndex,
			&i.X,
			&i.Y,
			&i.Width,
			&i.Height,
			&i.LayoutType,
			&i.Prompt,
			&i.CreatedAt,
			&i.UpdatedAt,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const updateMangaPanel = `-- name: UpdateMangaPanel :one
UPDATE manga_panels
SET order_index = COALESCE($2, order_index),
    x = COALESCE($3, x),
    y = COALESCE($4, y),
    width = COALESCE($5, width),
    height = COALESCE($6, height),
    layout_type = COALESCE($7, layout_type),
    prompt = COALESCE($8, prompt),
    updated_at = NOW()
WHERE id = $1
RETURNING id, page_id, order_index, x, y, width, height, layout_type, prompt, created_at, updated_at
`

type UpdateMangaPanelParams struct {
	ID         pgtype.UUID   `json:"id"`
	OrderIndex int32         `json:"order_index"`
	X          pgtype.Float8 `json:"x"`
	Y          pgtype.Float8 `json:"y"`
	Width      pgtype.Float8 `json:"width"`
	Height     pgtype.Float8 `json:"height"`
	LayoutType pgtype.Text   `json:"layout_type"`
	Prompt     pgtype.Text   `json:"prompt"`
}

func (q *Queries) UpdateMangaPanel(ctx context.Context, arg UpdateMangaPanelParams) (MangaPanel, error) {
	row := q.db.QueryRow(ctx, updateMangaPanel,
		arg.ID,
		arg.OrderIndex,
		arg.X,
		arg.Y,
		arg.Width,
		arg.Height,
		arg.LayoutType,
		arg.Prompt,
	)
	var i MangaPanel
	err := row.Scan(
		&i.ID,
		&i.PageID,
		&i.OrderIndex,
		&i.X,
		&i.Y,
		&i.Width,
		&i.Height,
		&i.LayoutType,
		&i.Prompt,
		&i.CreatedAt,
		&i.UpdatedAt,
	)
	return i, err
}
