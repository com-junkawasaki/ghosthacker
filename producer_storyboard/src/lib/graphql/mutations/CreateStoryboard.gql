mutation CreateStoryboard($input: CreateStoryboardInput!) {
	createStoryboard(input: $input) {
		id
		projectId
		title
		aspectRatio
		resolution
		durationSeconds
		numVariations
		createdAt
		updatedAt
	}
}
