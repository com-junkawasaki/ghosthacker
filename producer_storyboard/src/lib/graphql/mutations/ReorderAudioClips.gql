mutation ReorderAudioClips($trackId: ID!, $clipIds: [ID!]!) {
	reorderAudioClips(trackId: $trackId, clipIds: $clipIds) {
		id
		trackId
		startTimeSeconds
		durationSeconds
		audioType
		audioUrl
		audioDataId
		metadata
		createdAt
		updatedAt
	}
}
