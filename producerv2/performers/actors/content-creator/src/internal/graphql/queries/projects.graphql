query GetProjects {
  projects {
    id
    name
    description
    status
    createdAt
    updatedAt
  }
}

query GetProject($id: String!) {
  project(id: $id) {
    id
    name
    description
    status
    createdAt
    updatedAt
  }
}

